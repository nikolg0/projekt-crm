<h2>Lista klientów</h2>
<table>
  <thead>
    <tr>
      <th>Nazwa</th>
      <th>Adres</th>
      <th>NIP</th>
    </tr>
  </thead>

  <tbody>
    {{#each clients}}
      <tr>
        <td>{{this.name}}</td>
        <td>{{address.street}}
          {{address.suite}}
          {{address.city}}
          {{address.zipcode}}
        </td>
        <td>{{this.nip}}</td>
        <td>
          <a href="main/{{_id}}">Więcej</a>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>

<a href="/main/newClient">Dodaj Klienta</a>